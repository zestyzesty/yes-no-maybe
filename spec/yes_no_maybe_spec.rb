require 'spec_helper'

describe YesNoMaybe do
  it 'has a version number' do
    expect(YesNoMaybe::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
